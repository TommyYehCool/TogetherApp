# 🧪 Together App - 測試指南

## ⚠️ 前置需求

### 1. 安裝 Flutter

你的系統目前尚未安裝 Flutter。請依照以下步驟安裝：

#### Windows 安裝步驟

1. **下載 Flutter SDK**
   - 前往：https://docs.flutter.dev/get-started/install/windows
   - 下載最新的 Flutter SDK ZIP 檔案

2. **解壓縮 Flutter**
   - 將 ZIP 檔案解壓縮到你想要的位置（例如：`C:\src\flutter`）
   - **注意**：不要放在需要管理員權限的目錄（如 `C:\Program Files\`）

3. **設定環境變數**
   - 開啟「系統內容」→「進階」→「環境變數」
   - 在「使用者變數」中找到 `Path`
   - 新增 Flutter 的 bin 目錄路徑（例如：`C:\src\flutter\bin`）

4. **驗證安裝**
   ```bash
   flutter doctor
   ```

5. **安裝必要工具**
   根據 `flutter doctor` 的提示安裝：
   - Android Studio（用於 Android 開發）
   - Visual Studio（用於 Windows 桌面開發）
   - Chrome（用於 Web 開發）

---

## 🚀 快速測試方案

### 方案 A：使用 Chrome 瀏覽器測試（最快）

如果你已經安裝 Flutter，可以使用 Chrome 瀏覽器快速測試：

```bash
# 1. 安裝依賴
flutter pub get

# 2. 啟用 Web 支援
flutter config --enable-web

# 3. 在 Chrome 中執行
flutter run -d chrome
```

**注意**：Web 版本的 Google Maps 需要額外設定，但可以先看到基本 UI。

---

### 方案 B：使用 Android 模擬器測試

```bash
# 1. 安裝依賴
flutter pub get

# 2. 啟動 Android 模擬器
# 在 Android Studio 中啟動模擬器

# 3. 檢查可用裝置
flutter devices

# 4. 執行應用程式
flutter run
```

---

### 方案 C：使用實體手機測試（推薦）

#### Android 手機
1. 開啟「開發者選項」
2. 啟用「USB 偵錯」
3. 用 USB 連接電腦
4. 執行：
   ```bash
   flutter run
   ```

#### iOS 手機（需要 Mac）
1. 用 USB 連接 Mac
2. 在 Xcode 中設定開發者帳號
3. 執行：
   ```bash
   flutter run
   ```

---

## 📋 測試前檢查清單

### ✅ 必須完成
- [ ] 安裝 Flutter SDK
- [ ] 執行 `flutter doctor` 確認環境
- [ ] 執行 `flutter pub get` 安裝依賴
- [ ] 設定 Google Maps API Key（見下方）

### ⚙️ Google Maps API Key 設定

#### 取得 API Key
1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
2. 建立新專案
3. 啟用以下 API：
   - Maps SDK for Android
   - Maps SDK for iOS
4. 建立 API Key

#### 設定 API Key

**Android** - 編輯 `android/app/src/main/AndroidManifest.xml`：
```xml
<meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="你的_API_KEY_這裡"/>
```

**iOS** - 編輯 `ios/Runner/AppDelegate.swift`：
```swift
GMSServices.provideAPIKey("你的_API_KEY_這裡")
```

---

## 🧪 測試步驟

### 步驟 1：安裝依賴
```bash
cd C:\work\other\TogetherApp
flutter pub get
```

### 步驟 2：檢查裝置
```bash
flutter devices
```

應該會看到類似：
```
Chrome (web)        • chrome        • web-javascript • Google Chrome
Android SDK (mobile) • emulator-5554 • android        • Android 13
```

### 步驟 3：執行應用程式
```bash
# 自動選擇裝置
flutter run

# 或指定裝置
flutter run -d chrome
flutter run -d emulator-5554
```

### 步驟 4：測試功能

#### 基本功能測試
- [ ] 應用程式成功啟動
- [ ] 地圖正常顯示
- [ ] 看到 3 個測試活動標記（Mock 資料）
- [ ] 標記為膠囊形狀

#### 互動測試
- [ ] 點擊標記，底部面板滑出
- [ ] 查看活動詳情
- [ ] 點擊「建立活動」按鈕
- [ ] 填寫並提交表單
- [ ] 點擊「加入活動」按鈕

#### UI 測試
- [ ] 頂部搜尋列顯示正常
- [ ] 浮動按鈕位置正確
- [ ] 色彩符合設計規範（青綠色 #00D0DD）
- [ ] 圓角和陰影效果正確

---

## 🐛 常見問題排除

### 問題 1：Flutter 命令找不到
**解決方案**：
- 確認 Flutter 已加入環境變數 PATH
- 重新開啟終端機
- 執行 `flutter doctor` 驗證

### 問題 2：pub get 失敗
**解決方案**：
```bash
flutter clean
flutter pub get
```

### 問題 3：地圖無法顯示
**解決方案**：
- 檢查 Google Maps API Key 是否正確設定
- 確認 API Key 已啟用 Maps SDK
- 檢查網路連線

### 問題 4：標記無法顯示
**解決方案**：
```bash
flutter clean
flutter pub get
flutter run
```

### 問題 5：Android 編譯錯誤
**解決方案**：
- 確認 Android SDK 已安裝
- 檢查 `android/app/build.gradle` 的 minSdkVersion（應為 21）
- 執行：
  ```bash
  cd android
  gradlew clean
  cd ..
  flutter run
  ```

### 問題 6：iOS 編譯錯誤（Mac）
**解決方案**：
```bash
cd ios
pod install
cd ..
flutter clean
flutter run
```

---

## 📊 測試報告範本

測試完成後，請記錄以下資訊：

```
測試日期：____________________
測試裝置：____________________
Flutter 版本：________________

✅ 成功項目：
- [ ] 應用程式啟動
- [ ] 地圖顯示
- [ ] 標記顯示
- [ ] 點擊互動
- [ ] 建立活動
- [ ] 加入活動

❌ 失敗項目：
- 

🐛 發現的問題：
- 

💡 改進建議：
- 
```

---

## 🔧 開發模式測試

### Hot Reload（熱重載）
在應用程式執行時，修改程式碼後按 `r` 即可立即看到變化。

### Hot Restart（熱重啟）
按 `R` 完全重啟應用程式。

### 除錯模式
```bash
flutter run --debug
```

### 效能分析
```bash
flutter run --profile
```

---

## 📱 不同平台測試重點

### Android 測試
- [ ] 權限請求正常（位置權限）
- [ ] 地圖顯示正常
- [ ] 標記點擊反應靈敏
- [ ] 滑動面板流暢

### iOS 測試
- [ ] 權限請求正常
- [ ] 地圖效能良好
- [ ] UI 符合 iOS 規範
- [ ] 手勢操作流暢

### Web 測試（Chrome）
- [ ] 基本 UI 顯示
- [ ] 響應式設計
- [ ] 瀏覽器相容性

---

## 🎯 測試目標

### MVP 階段（目前）
- ✅ 基本功能運作
- ✅ UI 顯示正確
- ✅ Mock 資料正常
- ✅ 無嚴重 Bug

### 下一階段
- ⏳ 真實 API 整合測試
- ⏳ 效能測試
- ⏳ 壓力測試
- ⏳ 使用者測試

---

## 📞 需要協助？

### 文件參考
- **快速開始**：`QUICK_START.md`
- **設定指南**：`SETUP_GUIDE.md`
- **常見問題**：`SETUP_GUIDE.md` 的 FAQ 章節

### 外部資源
- [Flutter 官方文件](https://flutter.dev/docs)
- [Flutter 中文網](https://flutter.cn/)
- [Google Maps Flutter](https://pub.dev/packages/google_maps_flutter)

---

## ✨ 測試技巧

### 1. 使用 Flutter DevTools
```bash
flutter pub global activate devtools
flutter pub global run devtools
```

### 2. 查看日誌
```bash
flutter logs
```

### 3. 截圖
在應用程式執行時按 `s` 可以截圖。

### 4. 效能分析
按 `P` 可以顯示效能覆蓋層。

---

**祝測試順利！** 🚀

如有任何問題，請參考 `SETUP_GUIDE.md` 或 `QUICK_START.md`。
